AWater.AOcean.OceanMaskManager = function(cameraEl, maskLayerIndex){
  //Create render 10 render targets, each representing regions twice as large
  //as the previous region starting at 10m.

  //Create an orthographic camera

  this.tick(){
    //Every frame, run the first 2 frames.

    //Every other frame, run the second 2 frames.

    //Every third frame, run the third 2 frames.

    //Every fourth frame, run the fourth 2 frames.

    //Every fifth frame, run the last 2 frames.

  }
}
